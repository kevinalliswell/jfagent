# Domain Caddy serves; an A record must point here before first start so
# automatic HTTPS (Let's Encrypt) can issue the certificate.
DOMAIN=your-domain.example.com

# Optional: notification email for certificate issues.
#ACME_EMAIL=you@example.com
